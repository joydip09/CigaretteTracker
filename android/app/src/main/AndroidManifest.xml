<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
      <!-- ① Home screen widget -->
<receiver
    android:name=".widget.SmokeWidget"
    android:exported="true"
    android:label="Smoke Tracker Widget">
    <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
        <action android:name="com.cigarettetracker.ACTION_LOG_CIGARETTE" />
        <action android:name="com.cigarettetracker.ACTION_UNDO_CIGARETTE" />
        <action android:name="com.cigarettetracker.ACTION_UNDO_EXPIRED" />
    </intent-filter>
    <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/smoke_widget_info" />
</receiver>

<!-- ② Lock screen widget -->
<receiver
    android:name=".widget.SmokeLockscreenWidget"
    android:exported="true"
    android:label="Smoke Tracker — Lock Screen">
    <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
        <action android:name="com.cigarettetracker.ACTION_LOG_FROM_LOCK" />
    </intent-filter>
    <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/lockscreen_widget_info" />
</receiver>
    </application>
</manifest>
